<h1>Todos os Tamanhos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_sizes.each do |product_size| %>
  <tr>
    <td><%= product_size.name %></td>
    <td><%= link_to 'Ver', product_size %></td>
    <td><%= link_to 'Editar', edit_product_size_path(product_size) %></td>
    <td><%= link_to 'Apagar', product_size, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Cadastrar Tamanho', new_product_size_path %>
