<h1>Listing product_bases</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_bases.each do |product_basis| %>
  <tr>
    <td><%= product_basis.name %></td>
    <td><%= link_to 'Show', product_basis %></td>
    <td><%= link_to 'Edit', edit_product_basis_path(product_basis) %></td>
    <td><%= link_to 'Destroy', product_basis, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product base', new_product_basis_path %>
