<h1>Listing product_bases</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_bases.each do |product_basis| %>
  <tr>
    <td><%= product_basis.name %></td>
    <td><%= link_to 'Ver', product_basis %></td>
    <td><%= link_to 'Editar', edit_product_basis_path(product_basis) %></td>
    <td><%= link_to 'Apagar', product_basis, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product base', new_product_basis_path %>
