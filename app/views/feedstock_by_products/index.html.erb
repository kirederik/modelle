<h3>Matéria prima: <%= Product.find(params[:product_id]).name %></h3>

<table>
  <tr>
    <th>Feedstock</th>
    <th>Quantity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @feedstock_by_products.each do |feedstock_by_product| %>
  <tr>
    <td><%= feedstock_by_product.feedstock.name %></td>
    <td><%= feedstock_by_product.quantity %></td>
    <td><%= link_to 'Show', product_feedstock_by_product_path(feedstock_by_product.product_id, feedstock_by_product.id) %></td>
    <td><%= link_to 'Edit', edit_product_feedstock_by_product_path(feedstock_by_product.product_id, feedstock_by_product.id) %></td>
    <td><%= link_to 'Destroy', destroy_feedstock_by_product_path(feedstock_by_product), method: :delete, data: { confirm: 'Are you sure?' } %></td>
    <td></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Cadastrar Matéria Prima por Produto', new_product_feedstock_by_product_path %>
